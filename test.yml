twisted:
  thread_pool_size: 10

auth:
  timeout_seconds: 1
  realm: 'localhost.localdomain'
  urls:
    - 'http://localhost:5000/auth'

cassandra:
  pool_size: 1
  max_connections_per_node: 1
  keyspace: 'test'
  servers:
    - - 'localhost'
      - 9160
